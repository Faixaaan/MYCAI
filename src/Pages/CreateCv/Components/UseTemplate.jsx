import React from "react";
import { Box, Typography } from "@mui/material";

const UseTemplate = () => {
  return (
    <Box sx={{ p: 4, textAlign: "center" }}>
      <Typography variant="h4">Create CV Page</Typography>
    </Box>
  );
};

export default UseTemplate;
